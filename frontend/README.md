# TaskFlow frontend

React and Vite client for TaskFlow.

```powershell
npm install
npm test
npm run dev
npm run build
```

Set `VITE_API_URL` to point at the FastAPI `/api` base URL. When the API cannot be reached, the interface displays a clearly marked sample workspace so designers and reviewers can still inspect the full dashboard and Kanban experience.
